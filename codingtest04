#include <iostream>
#include <vector>
using namespace std;

int main() {
//	ios::sync_with_stdio(false);
//	cin.tie(NULL);
//	cout.tie(NULL);
//
//	int N, M;
//	cout << "N, M: ";
//	cin >> N >> M;
//	vector<long> S(N, 0);
//	vector<long> C(M, 0);
//	long answer = 0;
//	S[0] = 0;
//
//	cout << "배열 입력: ";
//	for (int i = 1; i < N; i++) { //합배열 구하는 코드
//		int temp = 0;
//		cin >> temp;
//		S[i] = S[i - 1] + temp;
//	}
//	for (int i = 0; i < N; i++) {
//		int remainder = S[i] % M;
//		//hint: 0인 경우는 C[0], answer 둘 다 증가함.
//		if (remainder == 0)
//			answer++;
//		C[remainder]++;
//	}
//	for (int i = 0; i < M; i++) {
//		if (C[i] > 1)
//			answer = answer + (C[i] * (C[i] - 1)) / 2;
//	}
//	cout << answer;


//	ios::sync_with_stdio(false);
//	cin.tie(NULL);
//	cout.tie(NULL);
//
//	int S[10001] = { };
//	int suNo, quNo;
//	cin >> suNo >> quNo;
//
//	for (int i = 1; i <= suNo; i++) {
//		int temp;
//		cin >> temp;
//		S[i] += S[i - 1] + temp;
//	}
//	for (int i = 0; i < quNo; i++) {
//		int a, b;
//		cin >> a >> b;
//		cout << S[b] - S[a - 1];
//	}


	int N = 0, sum = 0; string numbers;
	cin >> N;

	for (int i = 0; i < numbers.length(); i++)
	{
		sum += numbers[i] - '0';
	}
	cout << sum;
}
